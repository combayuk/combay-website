Phase 21.1 — Build fix for duplicate ok property

Fixes Vercel TypeScript error in:
src/app/api/admin/product-images/queue/route.ts

Problem:
parkImageProcessingForV2() already returns { ok: true, ... }.
The API route then returned { ok: true, ...result, ... }, causing duplicate object property 'ok'.

Fix:
Return { ...result, message: ... } instead.

No Prisma schema change.

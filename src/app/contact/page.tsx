import type { Metadata } from "next";import { Suspense } from "react";import TopBar from "@/components/layout/TopBar";import Navigation from "@/components/layout/Navigation";import Footer from "@/components/layout/Footer";import ContactForm from "@/components/forms/ContactForm";import CmsPageLayout from "@/components/pages/CmsPageLayout";import { getSiteContent } from "@/lib/siteContent";export const dynamic="force-dynamic";export const metadata:Metadata={title:"Contact Combay",description:"Get in touch with Combay."};export default async function ContactPage(){const c=await getSiteContent();return <main><TopBar/><Navigation/><CmsPageLayout page={c.pages.contact} contact={c.contact} formTitle={c.pages.contact.sectionHeading}><Suspense fallback={<div className="text-gray-400 text-sm">Loading form...</div>}><ContactForm/></Suspense></CmsPageLayout>{c.contact.mapEmbedUrl&&<section className="pb-12 bg-white"><div className="max-w-7xl mx-auto px-4"><div id="map" className="rounded-2xl overflow-hidden border border-gray-200 shadow-card"><iframe src={c.contact.mapEmbedUrl} width="100%" height="280" style={{border:0}} allowFullScreen loading="lazy" referrerPolicy="no-referrer-when-downgrade" title="Combay location"/></div></div></section>}<Footer content={{description:c.footer.description,backgroundImageUrl:c.footer.backgroundImageUrl,contact:c.contact}}/></main>}

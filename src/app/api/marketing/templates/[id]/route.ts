import { NextResponse } from "next/server";
import { prisma, withDatabase } from "@/lib/db";
import { isValidTemplateTrigger } from "@/lib/marketingTemplates";
export const dynamic = "force-dynamic";
function cleanTemplate(body: any) { const name = String(body.name || "").trim(); const type = String(body.type || "general").trim() || "general"; const trigger = isValidTemplateTrigger(body.trigger) ? body.trigger : null; const subject = String(body.subject || "").trim(); const bodyText = String(body.body || "").trim(); if (!name) throw new Error("Draft name is required."); if (!trigger) throw new Error("Choose a valid trigger for this draft."); if (!subject) throw new Error("Draft subject is required."); if (!bodyText) throw new Error("Draft body is required."); return { name, type, trigger, subject, body: bodyText, ctaLabel: body.ctaLabel ? String(body.ctaLabel).trim() : null, ctaUrl: body.ctaUrl ? String(body.ctaUrl).trim() : null }; }
export async function PATCH(request: Request, { params }: { params: { id: string } }) { if (params.id.startsWith("system:")) return NextResponse.json({ ok: false, error: "Built-in draft templates cannot be edited. Apply it, then save as a custom draft." }, { status: 400 }); const body = await request.json().catch(() => null); if (!body) return NextResponse.json({ ok: false, error: "Invalid draft data." }, { status: 400 }); try { const data = cleanTemplate(body); const result = await withDatabase(async () => prisma.emailDraftTemplate.update({ where: { id: params.id }, data })); if (!result.ok) return NextResponse.json({ ok: false, error: result.reason }, { status: 500 }); return NextResponse.json({ ok: true }); } catch (error) { return NextResponse.json({ ok: false, error: error instanceof Error ? error.message : "Could not update draft." }, { status: 400 }); } }
export async function DELETE(_request: Request, { params }: { params: { id: string } }) { if (params.id.startsWith("system:")) return NextResponse.json({ ok: false, error: "Built-in draft templates cannot be deleted." }, { status: 400 }); const result = await withDatabase(async () => { await prisma.emailDraftTemplate.delete({ where: { id: params.id } }); return true; }); if (!result.ok) return NextResponse.json({ ok: false, error: result.reason }, { status: 500 }); return NextResponse.json({ ok: true }); }

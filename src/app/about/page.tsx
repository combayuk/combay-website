import type { Metadata } from "next";import TopBar from "@/components/layout/TopBar";import Navigation from "@/components/layout/Navigation";import Footer from "@/components/layout/Footer";import CmsPageLayout from "@/components/pages/CmsPageLayout";import { getSiteContent } from "@/lib/siteContent";export const dynamic="force-dynamic";export const metadata:Metadata={title:"About Combay",description:"About Combay industrial equipment specialists."};export default async function AboutPage(){const c=await getSiteContent();return <main><TopBar/><Navigation/><CmsPageLayout page={c.pages.about}/><Footer content={{description:c.footer.description,backgroundImageUrl:c.footer.backgroundImageUrl,contact:c.contact}}/></main>}

import type { Metadata } from "next";import TopBar from "@/components/layout/TopBar";import Navigation from "@/components/layout/Navigation";import Footer from "@/components/layout/Footer";import RepairForm from "@/components/forms/RepairForm";import CmsPageLayout from "@/components/pages/CmsPageLayout";import { getSiteContent } from "@/lib/siteContent";export const dynamic="force-dynamic";export const metadata:Metadata={title:"Repair Services",description:"Industrial equipment repair, calibration, installation and PPM."};export default async function RepairPage(){const c=await getSiteContent();return <main><TopBar/><Navigation/><CmsPageLayout page={c.pages.repair} pageKey="repair" visualWidgets={c.visualWidgets} formTitle={c.pages.repair.ctaHeading}><RepairForm/></CmsPageLayout><Footer content={{description:c.footer.description,backgroundImageUrl:c.footer.backgroundImageUrl,contact:c.contact}}/></main>}

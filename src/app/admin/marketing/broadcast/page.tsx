"use client";
import { useState, type FormEvent } from "react";import Link from "next/link";import { Mail } from "lucide-react";
export default function BroadcastPage(){const[subject,setSubject]=useState("");const[body,setBody]=useState("Dear {{name}},\n\nWe have a Combay update for registered customers.");const[ctaLabel,setCtaLabel]=useState("Browse stock");const[ctaUrl,setCtaUrl]=useState("/shop");const[respect,setRespect]=useState(true);const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function send(e:FormEvent){e.preventDefault();if(!confirm("Send this email to registered customers?"))return;setBusy(true);setMsg("");const r=await fetch("/api/marketing/broadcast",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({subject,body,ctaLabel,ctaUrl,respectPreferences:respect})});const d=await r.json().catch(()=>({}));setBusy(false);setMsg(d.ok?`Checked ${d.checked}; sent ${d.sent}; skipped ${d.skipped}; failed ${d.failed}.`:d.error||"Broadcast failed.");}return <div className="space-y-6"><div><p className="font-mono text-xs tracking-widest uppercase text-gray-400 mb-2">Marketing</p><h1 className="font-display font-900 text-3xl text-navy-950">Send custom email</h1><p className="text-sm text-gray-500 mt-1">Send a one-off email to registered customers. Unsubscribe/preferences are respected by default.</p><Link href="/admin/marketing" className="btn-secondary mt-4 inline-flex">Back to email automation</Link></div>{msg&&<div className="bg-white border border-gray-200 rounded-xl p-4 text-sm text-gray-700">{msg}</div>}<form onSubmit={send} className="grid lg:grid-cols-[1fr_420px] gap-6"><section className="bg-white border border-gray-200 rounded-2xl p-6 space-y-4"><label><span className="label">Subject *</span><input required className="input" value={subject} onChange={e=>setSubject(e.target.value)}/></label><label><span className="label">Body *</span><textarea required className="textarea min-h-[340px] font-mono text-sm" value={body} onChange={e=>setBody(e.target.value)}/></label><div className="grid md:grid-cols-2 gap-4"><label><span className="label">Button label</span><input className="input" value={ctaLabel} onChange={e=>setCtaLabel(e.target.value)}/></label><label><span className="label">Button URL</span><input className="input" value={ctaUrl} onChange={e=>setCtaUrl(e.target.value)}/></label></div><label className="flex items-center gap-2 text-sm font-display font-700"><input type="checkbox" checked={respect} onChange={e=>setRespect(e.target.checked)}/>Respect unsubscribe and marketing preferences</label><button className="btn-primary inline-flex items-center gap-2" disabled={busy}><Mail size={16}/>{busy?"Sending...":"Send custom email"}</button></section><aside className="bg-white border border-gray-200 rounded-2xl overflow-hidden h-fit"><div className="p-5 border-b"><img src="/images/combay-doc-logo.png" className="w-40 h-auto mb-4" alt="Combay"/><h2 className="font-display font-800 text-lg text-navy-950">{subject||"Email subject"}</h2></div><div className="p-5 text-sm leading-7 whitespace-pre-line">{body.replace(/{{\s*name\s*}}/gi,"John Smith")}</div>{ctaLabel&&<div className="px-5 pb-5"><span className="inline-block rounded-lg bg-accent px-4 py-2 text-sm font-display font-800 text-navy-950">{ctaLabel}</span></div>}<div className="p-4 bg-gray-50 border-t text-xs text-gray-500"><strong>Combay Limited</strong><br/>Unsubscribe or manage preferences</div></aside></form><div className="bg-amber-50 border border-amber-200 rounded-xl p-4 text-sm text-amber-900"><strong>Tokens:</strong> <code>{"{{name}}"}</code>, <code>{"{{email}}"}</code>, <code>{"{{company}}"}</code>, <code>{"{{shopUrl}}"}</code>, <code>{"{{portalUrl}}"}</code>, <code>{"{{contactUrl}}"}</code></div></div>}

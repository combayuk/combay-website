import type { Metadata } from "next";import TopBar from "@/components/layout/TopBar";import Navigation from "@/components/layout/Navigation";import Footer from "@/components/layout/Footer";import AssetForm from "@/components/forms/AssetForm";import CmsPageLayout from "@/components/pages/CmsPageLayout";import { getSiteContent } from "@/lib/siteContent";export const dynamic="force-dynamic";export const metadata:Metadata={title:"Asset Recovery",description:"Sell your surplus or unwanted industrial equipment to Combay."};export default async function AssetRecoveryPage(){const c=await getSiteContent();return <main><TopBar/><Navigation/><CmsPageLayout page={c.pages.assetRecovery} pageKey="assetRecovery" visualWidgets={c.visualWidgets} formTitle={c.pages.assetRecovery.ctaHeading}><AssetForm/></CmsPageLayout><Footer content={{description:c.footer.description,backgroundImageUrl:c.footer.backgroundImageUrl,contact:c.contact}}/></main>}
